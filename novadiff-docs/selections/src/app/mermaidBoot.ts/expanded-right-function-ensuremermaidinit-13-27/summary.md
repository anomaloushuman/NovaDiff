### Overview
The selected region in this diff represents a new function, `ensureMermaidInit`, which initializes Mermaid with the current theme. The function is added at line 14 of the file `src/app/mermaidBoot.ts`. This change introduces a new dependency on Mermaid and sets up its initialization process.

### Selected change
The selected region includes lines 14-29 of the file, which define the `ensureMermaidInit` function. The function is async and returns a promise that resolves when Mermaid has been initialized. It takes no arguments and does not have any local variables. The function first retrieves the current Mermaid theme using the `currentMermaidTheme()` function, then checks if it matches the previously initialized theme. If they match, the function returns immediately without doing anything. Otherwise, it imports Mermaid and initializes it with the current theme, suppressing error rendering and setting the font family to inherit. Finally, it sets the initialized theme to the current one.

### Semantic context
This change is part of the NovaDiff project, which aims to improve the performance of Mermaid diagrams by optimizing their initialization process. The `ensureMermaidInit` function is added to ensure that Mermaid is only initialized once per page load, rather than on every diagram render. This reduces the amount of time spent initializing Mermaid and improves the overall performance of the application.

### Risks & follow-ups
The addition of this function introduces a new dependency on Mermaid, which may cause issues in certain environments or configurations. However, the function is designed to be safe and will not cause any harm if Mermaid is not present. Additionally, the function is only called when necessary, so it should not have a significant impact on the overall performance of the application. In the future, we may want to consider adding additional checks to ensure that Mermaid is properly initialized before using it. We may also want to explore other ways to optimize the initialization process, such as caching the initialized Mermaid instance or using a different initialization method. However, these changes are outside the scope of this pull request and would require further investigation and testing.
