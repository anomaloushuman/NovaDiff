### What it does`PrefetchSummaryJob` is a plain data holder used by the CLI to schedule a pre‑fetch operation for a repository subtree.- `path` stores the filesystem path of the target subtree.- `kind` indicates the type of change (`ChangeKind`) that triggered the job.- `depth` records how deep the subtree is relative to the repository root, which can influence pre‑fetch strategy.- `parent_subtree_weight` holds the cumulative weight of the parent subtree, likely used to prioritize or limit pre‑fetching based on size.### How it worksWhen the CLI detects a change, it creates an instance of `PrefetchSummaryJob` with the relevant metadata. This struct is then passed to the pre‑fetch worker queue. The worker reads the fields to determine which files to load into the cache, how aggressively to pre‑fetch, and whether to skip or defer the job based on `parent_subtree_weight`. The struct itself contains no methods; it is purely a transport object.### Notes for reviewers- The struct is immutable; all fields are public, so callers can construct it directly.- `ChangeKind` is referenced but not defined here; ensure its enum variants match the expected change types.- `depth` and `parent_subtree_weight` are `usize`; confirm that overflow is impossible in the expected use cases.- No validation logic is present; consider adding a constructor or `impl` block if invariants need enforcement.- The struct is lightweight and serializable, making it suitable for inter‑thread communication.
