### What it does`PrefetchSummaryQueueOut` aggregates the results of a pre‑fetch operation that prepares jobs for a diff comparison.- `queue` holds the prioritized jobs that will actually be processed, truncated to a configurable `limit` to keep IPC payloads small.- The remaining fields (`input_changes`, `skipped_gitignore`, `skipped_novadiff_docs`, `skipped_non_text`, `eligible_changes`, `returned`) are integer counters that record how many rows were received, filtered, or ultimately returned.### How it worksDuring pre‑fetch, each row from the compare engine is examined.1. **`input_changes`** is incremented for every row received.2. Rows that match a `.gitignore` or `.git/info/exclude` rule are counted in **`skipped_gitignore`** and omitted from further processing.3. Rows whose path falls under the `novadiff-docs/` bundle are counted in **`skipped_novadiff_docs`**.4. Rows that are binary or not valid UTF‑8 are counted in **`skipped_non_text`**.5. After these filters, the remaining rows are considered **eligible**; their count is stored in **`eligible_changes`**.6. Jobs are then created for each eligible row, prioritized, and pushed into **`queue`** until the `limit` is reached.7. **`returned`** mirrors `queue.len()` to confirm how many jobs were actually handed off.### Notes for reviewers- The struct itself contains no logic; it is purely a data container.- The snippet does not show how `limit` is defined or how the prioritization algorithm works—those details are elsewhere.- It is unclear whether `eligible_changes` includes rows that were later dropped due to the `limit`; the comment suggests it counts rows *after* gitignore and docs filtering but *before* truncation.- No visibility modifiers are shown; assume the struct is public to the IPC boundary.
