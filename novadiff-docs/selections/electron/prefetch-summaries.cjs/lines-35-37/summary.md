### What it does`getPrefetchedSummary` retrieves a pre‑fetched summary for a given relative path. It simply looks up the value in a map and returns it.### How it worksThe function accepts a single argument, `relPath`. It calls `summaryByPath.get(relPath)` and returns the result.- `summaryByPath` is expected to be a `Map` (or Map‑like object) that stores summary objects keyed by relative paths.- `Map.get` returns the value associated with the key or `undefined` if the key is absent.- The function has no side effects and performs no additional logic beyond the lookup.### Notes for reviewers- The snippet does not show how `summaryByPath` is defined or populated; its type and contents are unknown from the snippet.- No error handling is present; callers must be prepared for a possible `undefined` return value.- Because the function is pure, it can be inlined or memoized if performance profiling indicates a bottleneck.- Ensure that `relPath` is consistently normalized (e.g., no leading slashes, consistent case) to match the keys stored in `summaryByPath`.
