### What it does`sendKnowledgeGraphProgress` forwards progress updates from the main process to a renderer window. It accepts an `event` (typically an IPC event) and a `payload`. If the payload is a string, it parses a “current/total” pattern and sends a structured message; otherwise it forwards the payload unchanged, defaulting to an empty message when `payload` is `null` or `undefined`.### How it works1. `const wc = event?.sender;` grabs the `webContents` that emitted the event.2. `if (!wc || wc.isDestroyed()) { return; }` exits early if the target window is missing or already closed.3. When `payload` is a string, it runs `payload.match(/(\d+)\s*\/\s*(\d+)/)` to extract two numbers.4. It then calls `wc.send("knowledge-graph-progress", { … })` with: * `message`: the original string, * `phase`: hard‑coded `"extract"`, * `current` and `total`: parsed numbers or `undefined` if the regex fails.5. If `payload` is not a string, it sends it directly (or `{ message: "" }` if falsy) via the same channel.### Notes for reviewers* The function assumes `event.sender` is a `webContents` instance; the snippet does not verify its type.* The regex only captures digits; non‑numeric progress strings will result in `current`/`total` being `undefined`.* The hard‑coded `"extract"` phase suggests this helper is tied to a specific workflow; consider parameterizing it if reuse is needed.* No error handling is present for malformed payloads beyond the regex check.* Unknown from the snippet: the broader context of how `sendKnowledgeGraphProgress` is invoked and what the renderer expects.
