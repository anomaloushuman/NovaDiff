### What it does`sendGithubAuthProgress` sends a progress update to the renderer process during GitHub authentication.It takes an `event` (typically an IPC event) and an optional `payload`. If the event’s sender is a valid, non‑destroyed `webContents`, it forwards the payload—defaulting to an empty object—on the `"github-auth-progress"` channel.### How it works```jsfunction sendGithubAuthProgress(event, payload) { const wc = event?.sender; // Grab the webContents that emitted the event if (!wc || wc.isDestroyed()) { // Abort if no sender or it’s already closed return; } wc.send("github-auth-progress", payload ?? {}); // Send IPC message}```* `event?.sender` uses optional chaining to safely access `sender`.* `wc.isDestroyed()` checks whether the webContents has been closed.* `wc.send` is the Electron IPC method that delivers the message to the renderer.### Notes for reviewers* **Unknown from the snippet**: the exact type of `event` (e.g., `IpcMainEvent`), the type of `wc` (likely `WebContents`), and the signature of `wc.send`.* **Unknown**: whether `payload` is always an object, or if other types could be passed.* **Unknown**: error handling—`wc.send` may throw or return a promise that could reject.* **Unknown**: side effects beyond
