### What it does`extractAttrProperties` takes a TreeSitter node that represents a method call and returns an array of attribute names that were passed as symbol arguments. For example, given a call node for `foo(:name, :age)`, it will return `["name", "age"]`.### How it works1. It initializes an empty array `properties`.2. It retrieves the child node that holds the call’s arguments via `callNode.childForFieldName("arguments")`. If this node is missing, it immediately returns the empty array.3. It loops over each child of the arguments node (`args.childCount`).4. For each child, it checks whether the node exists and its `type` equals `"simple_symbol"`.5. When a simple symbol is found, it removes the leading colon by slicing the node’s text (`child.text.slice(1)`) and pushes the resulting string into `properties`.6. After iterating all children, it returns the populated `properties` array.### Notes for reviewers- The function has no side effects; it only reads from the provided `callNode`.- It assumes that `child.text` contains the raw source text of the symbol, which is typical for TreeSitter Ruby nodes.- The implementation ignores any arguments that are not simple symbols (e.g., literals, method calls).- If the arguments node is absent, the function correctly returns an empty array, which is useful for calls without symbol arguments.- Unknown from the snippet: the exact behavior of `childForFieldName`, `childCount`, and `child.text` depends on the TreeSitter Ruby grammar, but the code follows the standard API for accessing node children.
