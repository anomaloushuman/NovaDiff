### Overview
- New file `packages/graph-core/src/languages/alphabetical-catalog.generated.ts` added (R1‑673).  
- Auto‑generated by `scripts/generate-alphabetical-catalog.mjs`; header comment marks it as non‑editable (R4).  
- Exports `alphabeticalCatalogEntries: LanguageCatalogEntry[]` (R5) after importing the type from `./catalog-types.js` (R2).  
- Contains 667 `LanguageCatalogEntry` objects sorted alphabetically by `displayName` (R4).

### Key changes
- File addition: `alphabetical-catalog.generated.ts` (R1).  
- Import: `import type { LanguageCatalogEntry } from "./catalog-types.js";` (R2).  
- Export: `export const alphabeticalCatalogEntries: LanguageCatalogEntry[] = [` (R5).  
- New entries:  
  - `{ id: "ql", displayName: ".QL", extensions: [] }` (R6).  
  - `{ id: "formula", displayName: "@Formula", extensions: [] }` (R7).

### Impact
- Runtime catalog of 667 languages is now available for language‑catalog consumers.  
- Two new language entries (`.QL` and `@Formula`) are included with empty extensions.

### Risks & follow‑ups
- Bundle size may increase due to the large array; monitor production builds.  
- Ensure `scripts/generate-alphabetical-catalog.mjs` stays in sync to regenerate the file when language data changes.  
- Add tests to verify presence and ordering of the new entries.
