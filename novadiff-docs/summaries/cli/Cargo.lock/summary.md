### Overview  
A new file `cli/Cargo.lock` has been added to the repository (line range R1‑R379). It is generated by Cargo and declares the lockfile format `version = 4`.

### Key changes  
- The file begins with the autogenerated header lines (R1‑R3) and lists each dependency in a `[[package]]` section.  
- Each section contains `name`, `version`, `source`, `checksum`, and a `dependencies` array when applicable.  
- The lockfile records the exact versions of all crates used by `novadiff-cli`, including transitive dependencies such as `aho-corasick`, `block-buffer`, `regex`, `serde`, etc.

### Impact  
- **Deterministic builds** – Cargo will resolve dependencies to the exact versions recorded, eliminating nondeterministic resolution.  
- **Build speed** – Subsequent builds can skip dependency resolution, potentially speeding up the build after the first run.  
- **CI** – Existing CI jobs that run `cargo fetch` or `cargo build` will automatically use this lockfile; no script changes are required.  
- **Version drift** – If `Cargo.toml` changes, the lockfile must be regenerated with `cargo update` to stay in sync.

### Risks & follow‑ups  
- **Merge conflicts** – The lockfile is large; concurrent edits may cause conflicts that need manual resolution.  
- **Stale lockfile** – Verify the lockfile matches `Cargo.toml` by running `cargo check` after any dependency change.  
- **CI cache** – Ensure CI caching includes `cli/Cargo.lock` to avoid unnecessary re‑downloads.  
- **Security** – Checksums are present; any tampering would be detected during `cargo fetch`.
